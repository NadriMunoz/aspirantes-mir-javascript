function sum (num){
    let arr = 0;
    for (let i = 0; i < num.arr.length; i++){
        arr += num[i]
    }
    return (arr);
}


// escribe la función sum acá

console.log(arr([1, 2, 3])) // 6
console.log(arr([10, 8, 12, 5])) // 35
console.log(arr([])) // 0